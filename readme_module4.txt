Пояснение по пунктам лабораторной работы №11: 
-задание1;
-задание2;
-JS;

Пояснение по пунктам лабораторной работы №12:
В разделе Меню находятся 
1) В подменю РУС находится гиперссылка "Открыть новое окно", при нажатии на которую открывается новое окно с некоторым текстом (external.js).
2-3) В поддменю Soile находится форма обратной связи в подменю Ошибка с проверкой валидации, создан пользовательский объект, в который сохраняются данные,введенные пользователем. При нажатии на кнопку эти данные отображаются на странице (error.html основной файл, validation.js, formHandler.js, styles.css вспомогательные файлы)

Пояснение по пунктам лабораторной работы №13:
1) В подменю РУС находится карта объекта(страница книги с информацией). При наведении курсора мыши на область карты изменяется ее цвет, появляется всплывающие блоки с информацией (map.html основной файл, stylemap.css, map.js вспомогательные файлы).
2) В Soile находится веб- страница с текстом при наведении мыши на слово КОТ, слово исчезает и появляется изображение кота (soile.html основной файл).

Пояснение по пунктам лабораторной работы №14:
В подменю РУС находится "Диаграмма" график в форме и по данным, соответствующим тематике сайта. Тема "Распространение знания казахского языка среди различных народов"(взяты лишь первые 5) (di.html основной файл, styles.css, scriptt.js, вспомогательные файлы).